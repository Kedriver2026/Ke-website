const launcher=document.getElementById('kateLauncher');const panel=document.getElementById('katePanel');const closeBtn=document.getElementById('kateClose');const body=document.getElementById('kateBody');const form=document.getElementById('kateForm');const input=document.getElementById('kateInput');function setOpen(open){panel.classList.toggle('open',open);panel.setAttribute('aria-hidden',String(!open));launcher.setAttribute('aria-expanded',String(open));if(open)setTimeout(()=>input.focus(),100)}launcher.addEventListener('click',()=>setOpen(!panel.classList.contains('open')));closeBtn.addEventListener('click',()=>setOpen(false));const replies={status:'I can help with your delivery status. Live lookup is not connected yet, so I won’t guess. Once secure verification is enabled, I’ll verify your delivery before showing its current status.',attempted:'I can help explain an attempted delivery. Once live delivery records are connected, I’ll verify your delivery and tell you whether an attempt was recorded, when it happened, and the appropriate next step.',problem:'I’m here to help with a delivery problem. For now, please contact K&E Delivery so our team can review the delivery record directly.',quote:'I can help you request a K&E Delivery service quote. Tell me about the airport, delivery area, expected volume, and service you need.',areas:'K&E provides airport luggage and last-mile delivery services. I can help with service-area and partnership questions.',drivers:'Interested in driving with K&E? I can help with independent driver opportunity questions.',other:'I’m the K&E Delivery Assistant. I can help with delivery questions, attempted deliveries, service information, quotes, delivery areas, and driver opportunities.'};function addMessage(text,type='bot'){const msg=document.createElement('div');msg.className='kate-message '+(type==='user'?'kate-user':'kate-bot');msg.textContent=text;body.appendChild(msg);body.scrollTop=body.scrollHeight}function answerText(text){const q=text.toLowerCase();if(q.includes('attempt')||q.includes('nobody home')||q.includes('missed delivery'))return replies.attempted;if(q.includes('status')||q.includes('where')||q.includes('track')||q.includes('bag'))return replies.status;if(q.includes('problem')||q.includes('damage')||q.includes('wrong')||q.includes('issue'))return replies.problem;if(q.includes('quote')||q.includes('price')||q.includes('cost')||q.includes('rate'))return replies.quote;if(q.includes('area')||q.includes('city')||q.includes('serve')||q.includes('service'))return replies.areas;if(q.includes('driver')||q.includes('job')||q.includes('work')||q.includes('apply'))return replies.drivers;if(/^(hi|hello|hey|hola)\b/.test(q))return 'Hi! I’m KatE. I’m here to help with K&E Delivery. What can I help you with today?';return 'I can help with K&E Delivery questions. You can ask me about delivery status, attempted deliveries, delivery problems, quotes, service areas, or driver opportunities.'}document.querySelectorAll('#kateOptions button').forEach(btn=>btn.addEventListener('click',()=>addMessage(replies[btn.dataset.topic])));form.addEventListener('submit',e=>{e.preventDefault();const text=input.value.trim();if(!text)return;addMessage(text,'user');input.value='';setTimeout(()=>addMessage(answerText(text)),180)});